# Environment Configuration Example File
# 
# INSTRUCTIONS:
# 1. Copy this file to .env: cp .env.example .env
# 2. Update NODE_ENV and REACT_APP_ENV based on your environment:
#    - development: For local development
#    - testing: For running tests
#    - production: For production deployment
# 3. Change JWT_SECRET to a secure random string in production
# 4. Modify other values as needed for your setup
#
# Note: The .env file is in .gitignore and won't be committed to Git

# ==========================================
# Node.js Environment
# ==========================================
# Options: development, testing, production
# Change this to 'testing' when running tests, 'production' for deployment
NODE_ENV=development

# ==========================================
# PostgreSQL Database Configuration
# ==========================================
POSTGRES_USER=jduser
POSTGRES_PASSWORD=jdpassword
POSTGRES_DB=jdanalyzer
POSTGRES_PORT=5432

# ==========================================
# Server Configuration
# ==========================================
SERVER_PORT=3001

# ==========================================
# Client Configuration
# ==========================================
CLIENT_PORT=3000

# React App Environment
# Options: development, testing, production
# Change this to match NODE_ENV for consistency
REACT_APP_ENV=development

# API URL for React client to connect to backend
REACT_APP_API_URL=http://localhost:3001/api

# ==========================================
# JWT Authentication Configuration
# ==========================================
# ⚠️ IMPORTANT: Change this to a secure random string in production!
# Generate a secure secret: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=24h
